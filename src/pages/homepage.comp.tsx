import PageWrapper from "../components/pagewrapper/pagewrapper.comp";

interface Props {}

const HomePage = (props: Props) => {
  return <PageWrapper>homepage with list of todo for today</PageWrapper>;
};

export default HomePage;
