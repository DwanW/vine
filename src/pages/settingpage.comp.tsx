import PageWrapper from "../components/pagewrapper/pagewrapper.comp";
// import { useAppSelector } from "../util/hooks";

interface Props {}

const SettingPage = (props: Props) => {
  return (
    <PageWrapper>
      this is the setting page, handling preferences, customizations and lists
    </PageWrapper>
  );
};

export default SettingPage;
